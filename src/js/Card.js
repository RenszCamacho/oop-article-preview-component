class Title {
  constructor(poster, title, description, image, name, date) {
    this.poster = poster;
    this.title = title;
    this.description = description;
    this.image = image;
    this.name = name;
    this.date = date;
  }
}

export default Title;
